{
  "name": "angular-app",
  "version": "0.0.0",
  "description": "dashboad app in angular",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  },
  "dependencies": {
    "angular-chart.js": "^1.0.0-alpha8"
  }
}
